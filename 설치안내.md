# TOURSTORY OP SYSTEM — 팀원용 설치 안내

## 먼저 알아두실 것

화면은 **인터넷 주소 하나**로 다 같이 씁니다. 설치할 게 없습니다.

봇은 **각자 PC 에서** 돕니다. OTA 로그인이 각자 Chrome 에 들어 있기 때문입니다.
그래서 PC 마다 `Agent` 하나만 켜 두시면 됩니다.

```
   화면 (인터넷)  ──────────  내 PC 의 Agent  ──→  내 Chrome  ──→  OTA
   다 같이 보는 곳            내가 켜 두는 창          내 로그인
```

> **로그인 정보는 내 PC 를 떠나지 않습니다.** 인터넷으로 오가는 것은
> '무슨 상품 몇 개' 뿐입니다.

---

## 화면 주소

**https://tsopsystem-dtmqmcjiae3cb8shgq8b56.streamlit.app**

즐겨찾기에 넣어두세요. 설치 없이 바로 열립니다.

---

## 내 PC 에 Agent 설치 (한 번만, 10분)

### 1. Python 설치

https://www.python.org/downloads/ 에서 받습니다.

⚠️ 설치 화면 맨 아래 **"Add python.exe to PATH"** 에 **반드시 체크**하세요.
이거 빼먹으면 나중에 안 됩니다.

### 2. 파일 받기

화면의 **필요 파일 다운로드** 에서 **파일 받기 (ZIP)** 를 누릅니다.

받은 압축을 풀어 원하는 곳에 둡니다 (바탕화면 추천).

> 저장소 주소: https://github.com/riokim-source/TS_OP_AGENT
> 사내망에서 막히면 화면의 [저장소가 안 열릴 때] 에서 바로 받으세요.

### 3. `설치.bat` 더블클릭

필요한 것을 알아서 채웁니다. 3~5분 걸립니다.

### 4. 열쇠 파일 확인

`hub\data\firebase_service_account.json` 이 있어야 합니다.

없으면 관리자에게 요청하세요. **이 파일은 메신저나 메일로 보내지 마세요.**
사내 공유 폴더나 USB 로만 주고받습니다.

### 5. Chrome 로그인

`Agent 켜기.bat` 을 켜 두고, 화면의 **Chrome 로그인** 에서 지역별로
`실행` 을 누릅니다. Chrome 창이 열리면 각 OTA 에 로그인하세요.

한 번 로그인하면 계속 유지됩니다.

---

## 매일 아침 (30초)

### 1. `Agent 켜기.bat` 더블클릭

검은 창이 뜹니다. **업무 중에는 닫지 마세요.** 닫으면 내 PC 로 오는 작업이
실행되지 않습니다.

```
==============================================================
 TOURSTORY OP - Agent
==============================================================
  이 PC   : DESKTOP-ABC/hong
  중계    : firebase
  대기 중...
```

`대기 중` 이 보이면 준비 끝입니다.

### 2. 화면 열기

즐겨찾기의 주소를 엽니다. 첫 화면에 이렇게 나오면 연결된 겁니다.

```
보고 있는 PC: DESKTOP-ABC/hong
실행 중 5/5
```

### 3. 순서대로

| 순서 | 메뉴 | 하는 일 |
|---|---|---|
| 1 | **Chrome 로그인** | 전부 `로그인됨` 인지 확인 |
| 2 | **Last Minute › Inventory 마감** | 날짜가 `내일` 인지 보고 실행 (**10시 전**) |
| 3 | **Last Minute › 수량 수집 · 오픈** | 예약 파일 올림 → 수량 입력 → Office/OP 텍스트 복사 → `오픈 대상 확인` → `자동 오픈 실행` |
| 4 | **실행 기록** | 실패한 게 있으면 확인하고 손으로 처리 |

---

## 안 될 때

| 증상 | 원인 | 해결 |
|---|---|---|
| **연결된 PC 가 없습니다** | Agent 를 안 켰음 | `Agent 켜기.bat` 더블클릭 |
| **실행 중 0/5** | Chrome 이 꺼져 있음 | `Chrome 로그인` 에서 `실행` |
| **로그인 안 됨** | 세션 만료 | 해당 Chrome 창에서 다시 로그인 |
| Agent 창이 **바로 닫힘** | Python 이 없거나 PATH 미체크 | Python 다시 설치 (PATH 체크) |
| **Firebase 서비스 계정이 없습니다** | 열쇠 파일 없음 | 4번 단계 참고 |
| 화면이 **접근 권한 없음** | 앱 공유 설정 | 관리자에게 요청 |

---

## 하지 말아야 할 것

- **열쇠 파일(`firebase_service_account.json`)을 메신저·메일·카톡으로 보내지 마세요.**
  이 파일 하나면 아무 PC 나 우리 시스템에 붙을 수 있습니다.
- 화면 주소를 외부에 알리지 마세요. 지금은 주소만 알면 누구나 들어옵니다.
- 마감은 **10시 전**에 끝내세요. 지나면 예약이 들어온 뒤 마감하게 됩니다.

---

## 혼자 쓰던 방식으로 되돌리기

중앙 화면에 문제가 생기면 예전처럼 내 PC 에서 직접 열 수 있습니다.

`OP System 열기.bat` 더블클릭 → `http://localhost:8610`

중앙을 거치지 않고 이 PC 에서 바로 돕니다. 급할 때 쓰는 비상구입니다.
